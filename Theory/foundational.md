# Java

```java
String text = "apple banana cherry";
String[] parts = text.split(" ");

```

```java
HashSet<String> set = new HashSet<>;

```

Check if an element exist

```java
set.contains("Value to check");

// To add a value use 

set.add("Value");

```

How to iterate over by letter to letter

```java
String word = "hello"
for(int i = 0; i< word.length(); i++) {
    char ch = word.charAt(i);
    //DO whatever you wnant with that ch.
}
```
